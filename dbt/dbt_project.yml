name: 'event_driven_pipeline'
version: '1.0.0'
config-version: 2

profile: 'event_driven_pipeline'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  event_driven_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
